{
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
